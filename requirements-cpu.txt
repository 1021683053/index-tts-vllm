# Native CPU runtime for IndexTTS2. This deliberately does not install vLLM.
# Both indexes are domestic Aliyun mirrors. The second one supplies CPU-only
# PyTorch wheels for Linux x86_64.
--index-url https://mirrors.aliyun.com/pypi/simple
--extra-index-url https://mirrors.aliyun.com/pytorch-wheels/cpu

torch
torchaudio
transformers
safetensors
accelerate
descript-audiotools==0.7.2
matplotlib==3.8.4
omegaconf
sentencepiece
librosa
gradio
modelscope
munch==4.0.0
loguru
python-multipart
soundfile
pandas

WeTextProcessing; platform_machine != "Darwin"
wetext; platform_system == "Darwin"
