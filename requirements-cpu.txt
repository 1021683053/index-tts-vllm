# Native CPU runtime for IndexTTS2. This deliberately does not install vLLM.
# Install the pinned CPU-only torch and torchaudio wheels before this file; see
# Dockerfile.cpu and README.md. Keeping their dedicated mirror out of this file
# prevents uv from resolving unrelated packages (such as numpy) against it.
transformers
safetensors
accelerate
descript-audiotools==0.7.2
matplotlib==3.8.4
omegaconf
sentencepiece
librosa
gradio
modelscope
munch==4.0.0
loguru
python-multipart
soundfile
pandas

WeTextProcessing; platform_machine != "Darwin"
wetext; platform_system == "Darwin"
